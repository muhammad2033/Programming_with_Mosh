numbers = (1,2,4,5,6,7)
print(numbers[3])  # finding the value , at which index it locates

print(numbers.count(4))  # use for counting the element 

# tuple is immutable,nothing can change here such, remove , clear , pop , insert, append etc
# without 2 functions, index and count()